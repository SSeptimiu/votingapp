apiVersion: v1
kind: Service
metadata:
  name: voting-app-service
  labels:
    name: voting-service
    app: demo-voting-app

spec:
  type: LoadBalancer
  ports: 
  - port: 80
    targetPort: 80
  selector:
    name: result-voting-pod
    app: demo-voting-app
